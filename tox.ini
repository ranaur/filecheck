[tox]
envlist = py38, py311, py314
skip_missing_interpreters = true

[testenv]
skip_install = true
deps =
    pytest
    pytest-cov
commands =
    pytest tests/ --tb=short --cov=filecheck --cov-report=term-missing
